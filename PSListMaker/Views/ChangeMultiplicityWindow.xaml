<Window x:Class="PSListMaker.Views.ChangeMultiplicityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PSListMaker.Views"
        mc:Ignorable="d"
        Title="ChangeMultiplicityWindow" Height="200" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="Наименование" Margin="0 0 10 5"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="Кратность" Margin="10 0 10 0" HorizontalAlignment="Center"/>
                <TextBox x:Name="MultValue" Text="1" Width="40" Margin="10 2 10 0" TextChanged="Muliplicity_TextChanged"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" FlowDirection="RightToLeft">
            <Button Content="Отменить" Click="Close_Button_Click"/>
            <Button Content="Изменить" Click="Change_Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
