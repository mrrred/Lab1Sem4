<Window x:Class="PSListMaker.ComponentsList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PSListMaker"
        mc:Ignorable="d"
        Title="Список компонентов" Height="300" Width="340">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Добавить"></MenuItem>
            <MenuItem Header="Изменить"></MenuItem>
            <MenuItem Header="Отменить"></MenuItem>
            <MenuItem Header="Сохранить"></MenuItem>
            <MenuItem Header="Удалить"></MenuItem>

        </Menu>

        <Separator Height="3" Padding="0 5 0 5" Grid.Row="1"/>

        <ListBox Grid.Row="2">
            
        </ListBox>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Наименование" Margin="0 0 10 0"></TextBlock>
                <TextBox Width="100"></TextBox>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Тип" Margin="0 0 10 0"></TextBlock>
                <ComboBox Width="70"></ComboBox>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
